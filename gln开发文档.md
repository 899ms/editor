# GLN 光冷暖 Editor 开发文档

## 1. 文档定位

这是一个全新的 GLN 需求，以 Pascal Editor 为基础进行二次开发。原版
Editor 必须保留，GLN 在独立的 `feature/gln-editor` 分支和 Worktree 中开发，
不得覆盖原版应用，也不得破坏现有中文化修改。

第一版交付的是一个可实时编辑、保存和检查的光冷暖住宅场景编辑器。它不是一张
固定效果图，也不是不可编辑的整体三维模型。外机、缓冲水箱、水管和室内面板都
必须是正常场景节点，能够像原版 Editor 组件一样选择、移动、修改、删除、撤销和
保存。

本文件是产品范围、领域规则、架构边界、实施顺序和验收标准的共同基准。领域术语
见 `docs/gln/CONTEXT.md`，核心架构决策见
`docs/gln/adr/0001-isolated-gln-editor.md`。

## 2. 第一版目标

第一版必须完成：

- 保留原版 `apps/editor`，新增独立 `apps/gln-editor`。
- 静态加载不可由普通用户卸载的 GLN 插件。
- 支持导入住宅、AI 生成住宅，以及在既有住宅上配置光冷暖系统。
- 把 GLB、IFC 等整体住宅模型重建为可编辑建筑节点。
- 使用 Editor 已有组件生成墙体、楼板、天花、屋面、门窗、楼层和 Zone。
- 添加且只添加四类可见 GLN 资产：外机、水管、缓冲水箱、室内面板。
- 用不可见的 `gln:system` 节点保存系统级状态。
- 支持普通住宅默认一套系统，复杂住宅允许多套系统。
- 支持各有面板的空间独立设置目标温度、目标湿度和启停状态。
- 支持供回水拓扑、吊顶管路、墙内下降管和面板顶部接口。
- 支持编辑视图与运行预览，实时展示水流及能量传递方向。
- 使用独立 SQLite 数据库自动保存场景。
- 使用本机 Codex 生成受约束的 `ScenePlan`，预览后原子提交。

第一版不需要为了查看即时效果而导出。JSON 仅用于备份和迁移，不是场景运行的
前置步骤。

## 3. 科学表达边界

### 3.1 系统链路

```text
室外主机
  <-> 缓冲水箱
  <-> 供回水干管与支管
  <-> 室内贴墙面板
  <-> 房间表面与人体之间的能量传递
```

表达规则：

- 面板贴墙安装，不悬空，不表现为送风口或落地散热器。
- 水管默认走吊顶服务层，室内不出现无意义的明管。
- 面板顶部两侧连接：人在室内面对面板时，左侧为供水入口，右侧为回水出口。
- 左右定义使用面板局部坐标，换墙或翻面后仍保持局部左进右出。
- 夏季表达空间余热向冷面板传递。
- 冬季表达面板向房间表面和人体传递温暖。
- 不使用空调送风箭头解释辐射面板。
- 流动、波纹和能量点只表达方向与关系，不冒充计算结果。

### 3.2 温湿度

- 只有至少安装一块面板的 Zone 才能启用独立温湿度设置。
- 设置属于 Zone，不属于单块面板；同一 Zone 的多块面板共享一组设定。
- 第一版只保存目标温度、目标湿度和启停状态。
- 未提供目标值时显示“未设置”，不得自行生成 `24°C / 50%` 等结果。
- 模板可以提供可编辑建议值，但必须明确标为模板建议。
- 删除 Zone 中最后一块面板后，原设置保留但禁用。
- 第一版不显示伪造的实时温湿度、变化曲线、流量、温差、负荷或能耗。

辐射面板承担温度调节，不应默认宣称面板独立完成除湿。常规辐射系统的潜热可能
由独立新风除湿系统承担，部分产品也可能采用其他经过验证的调湿方案。第一版只
记录目标湿度；在缺少具体产品资料时，不宣称四类资产已经实现主动除湿。

### 3.3 暂不计算

以下能力进入后续待办，不属于第一版计算结果：

- 建筑负荷计算。
- 水力平衡。
- 真实温湿度演算。
- 露点与结露风险计算。
- 设备容量选型、能耗和控制策略计算。

UI 不显示“演示模式”标签，但必须通过字段命名、帮助信息和验收规则避免把方向
动画描述成工程仿真。

## 4. 架构决策

### 4.1 应用隔离

```text
apps/editor                 原版 Editor，保持现有行为
apps/gln-editor             GLN 专用应用
packages/plugin-gln         GLN 节点、工具、面板和可视化
packages/core               仅接受通用场景与插件能力
packages/editor             仅接受通用编辑器能力
packages/mcp                仅接受通用存储、校验和受限 Agent 接口
```

`apps/gln-editor` 从现有 Editor 组合能力，不复制一套独立 Three.js 内核。GLN
插件在启动时静态注册并默认安装，普通用户不能卸载。原版 `apps/editor` 不加载
GLN 插件。

### 4.2 Three.js 边界

继续使用仓库已有 Three.js、React Three Fiber、节点注册表、工具和系统机制：

- 参数化设备使用 `NodeDefinition.geometry` 或自定义 `renderer`。
- 水流和能量动画使用插件 `system` 与运行时 store。
- 编辑工具使用 `tool`、`affordanceTools` 和现有空间查询。
- 2D/3D 都适用的移动、放置行为必须保持一致。
- 每帧动画状态不得写入场景图或污染撤销历史。

不重新开发裸 Three.js 编辑器，不把 GLN 业务字段写入内置节点联合类型，也不把
GLN 设备降级为普通 `item`。

### 4.3 宿主通用修改

GLN Worktree 可以修改共享包，但仅限所有插件都可复用的能力：

- 基于注册表的插件节点校验。
- 插件节点的保存、导入和 MCP 修改。
- 通用原子场景事务、恢复点和冲突处理。
- 插件节点的关系、端口、移动及放置扩展点。

GLN 名称、节点字段、安装规则和可视化不得进入通用包。

## 5. 现有仓库能力与缺口

### 5.1 可复用能力

| 需求 | 现有能力 |
| --- | --- |
| 建筑编辑 | building、level、wall、slab、ceiling、roof、door、window、zone |
| 三维画布 | Three.js / React Three Fiber |
| 编辑交互 | 选择、移动、旋转、复制、删除、吸附、撤销和重做 |
| 插件 | `Plugin`、`NodeDefinition`、renderer、system、tool、host panel |
| 拓扑 | typed ports 与 system graph |
| 保存 | SQLite SceneStore、场景 API、自动保存、版本号和乐观锁 |
| IFC | `apps/ifc-converter` 与 `packages/ifc-converter` |
| AI 编辑 | MCP 场景操作、模板和 live sync |

### 5.2 必须补齐

当前服务端仍可能用固定 `AnyNode` 联合类型验证场景。第一阶段必须让浏览器、
服务端、JSON 导入与 MCP 使用同一插件注册表和 Schema：

```text
读取 node.type
  -> 在 registry 查找 NodeDefinition
  -> 用 definition.schema 校验
  -> 未注册或非法节点拒绝保存
  -> 合法节点进入统一场景事务
```

不得通过跳过安全校验来兼容插件节点。

现有 IFC 转换器处于早期阶段，可以复用分类、定位和尺寸信息，但不能假设它已经
满足完整住宅重建。当前没有等价的 GLB 到可编辑节点转换器，需要新增几何分析与
复核流程。

现有节点不能冒充 GLN 资产：

- `hvac-equipment` 面向炉体、空气处理机和冷凝器，不是本项目水系统模型。
- `liquid-line` 是冷媒液管。
- `pipe-segment` 带排水、废水和通气语义。
- `item` 缺少系统、端口、Zone 和安装约束。

## 6. 领域模型

### 6.1 节点类型

```text
gln:system           不可见逻辑节点
gln:outdoor-unit     可见物理节点
gln:buffer-tank      可见物理节点
gln:hydronic-pipe    可见物理节点
gln:wall-panel       可见物理节点
```

不再增加第五类可见设备。未来若引入其他系统设备，必须单独变更需求，不能在第一版
暗中生成。

### 6.2 `gln:system`

`gln:system` 是单一系统级事实来源：

```ts
type GlnSystemNode = {
  type: 'gln:system'
  id: string
  name: string
  mode: 'cooling' | 'heating' | 'standby'
  zoneSettings: Record<
    string,
    {
      targetTemperatureC: number | null
      targetHumidityPercent: number | null
      enabled: boolean
      source: 'user' | 'template'
    }
  >
}
```

约束：

- 物理节点只保存 `systemId`，系统节点不重复保存成员 ID 列表。
- 成员关系按 `systemId` 派生。
- 连接关系按类型化端口与共享管线端点派生。
- 运行模式只存在于 `gln:system`，不得复制到外机、水箱或面板。
- 普通住宅默认一套系统；复杂住宅允许多套，用户可以调整或新增。

### 6.3 通用物理节点字段

四类物理节点至少包含：

```ts
type GlnPhysicalFields = {
  systemId: string
  lockedByUser: boolean
  presetId: string | null
  modelLabel: string | null
}
```

没有厂家资料时使用“通用占位预设”，明确显示“型号未指定”。AI 不得编造品牌、
型号、容量、性能或检修距离。

### 6.4 外机

`gln:outdoor-unit` 提供 `hydronic-supply` 和 `hydronic-return` 端口。

可自动放置区域：

- 室外地面。
- 空调设备平台。
- 生活阳台的设备区。
- 屋面设备区。

普通观景阳台只有满足通风、检修和安全条件时才允许。外机不得放入普通室内空间，
不得与外墙或其他构件相交。没有有效位置时请求用户指定。

### 6.5 缓冲水箱

`gln:buffer-tank` 至少提供主机侧和负载侧供回水端口。它只能自动放置在：

- 设备间或机房。
- 生活阳台的设备区。
- 靠外墙的服务设备区。

不得自动放置在客厅、卧室或书房。找不到有效区域时请求用户指定。冷热分层视觉
只解释储能和稳定输出，不表示流体计算结果。

### 6.6 水管

`gln:hydronic-pipe` 使用多段三维路径：

```ts
type GlnHydronicPipeNode = {
  type: 'gln:hydronic-pipe'
  systemId: string
  loop: 'supply' | 'return'
  path: Array<[number, number, number]>
  diameterMm: number | null
  insulated: boolean
  lockedByUser: boolean
}
```

多个管线节点通过共享端点形成干管、支管和立管。弯头、三通和连接件由路径与共享
端点自动生成，不作为第五类可见资产。

### 6.7 面板

`gln:wall-panel` 是贴墙参数化节点：

```ts
type GlnWallPanelNode = {
  type: 'gln:wall-panel'
  systemId: string
  wallId: string
  zoneId: string | null
  width: number
  height: number
  depth: number
  localSide: 'front' | 'back'
  lockedByUser: boolean
}
```

默认尺寸：

```text
宽 0.9m
高 2.5m
厚 0.12m
```

尺寸可按预设或用户输入调整。面板必须：

- 贴附有效墙面并沿墙移动。
- 避开门窗、墙外边界和其他安装冲突。
- 根据朝向确定所服务的 Zone，并写入 `zoneId`。
- 移动、翻面或换墙后重新计算 Zone。
- 归属不明确时请求用户选择，不能猜测。
- 在局部顶部左侧提供供水口，右侧提供回水口。

未进行负荷计算前，普通房间默认一块面板。模板、空间条件或用户操作可以增加，
但不得宣称数量满足真实负荷。

## 7. 管路与拓扑规则

### 7.1 标准路径

```text
外机
  -> 缓冲水箱
  -> 设备侧可见供回水
  -> 穿墙
  -> 吊顶供回水干管
  -> 各房间支管
  -> 面板所在墙内分别下降
  -> 面板顶部左进右出
```

完整路径始终保存在场景图中。设备侧默认可见，吊顶和墙内管段默认隐藏；“显示隐蔽
管线”检查开关可以临时显示全部路径。

### 7.2 路由约束

- 吊顶管路避开门窗洞口、梁柱、楼板边界和已有设备。
- 墙内下降管不得穿过门窗。
- 多楼层管路优先使用管井或设备墙形成立管。
- 一套系统可以服务多个楼层；复杂住宅也可拆成多套系统。
- 找不到无碰撞路径时进入人工复核，不得强行穿过构件。
- 供回水方向由拓扑与端口决定，不由颜色或画面左右位置决定。

### 7.3 删除和断连

删除设备、面板或管线前显示受影响连接。确认删除后：

- 未受影响节点继续保留。
- 断开的端口和管线进入“待修复”状态并明确标红。
- 不静默重连，也不连接到其他相邻设备。

## 8. 住宅来源与可编辑重建

### 8.1 来源

住宅可以来自：

- 用户在 Editor 中手工搭建。
- 导入 GLB。
- 导入 IFC。
- AI 根据文字、平面图或结构化需求生成。

无论来源如何，最终住宅必须由 Editor 已有正常节点构成，不能成为不可编辑整体。

### 8.2 第一版拆解范围

必须重建：

- building
- level
- wall
- slab
- ceiling
- roof
- door
- window
- zone

家具、装饰和灯具不是第一版强制范围。能匹配现有目录资产时可生成 `item`，无法
可靠匹配时进入复核队列。

IFC 可以使用分类、定位和尺寸提高重建准确度，但不保留、编辑或导出 BIM 属性。
GLB 使用几何分析、低分辨率视图和 AI 重建。

### 8.3 置信度与复核

- 高置信度结果直接生成可编辑节点草案。
- 低置信度结果进入复核队列。
- 原模型可临时作为半透明对照层。
- 正式场景最终由可编辑节点组成，对照层默认隐藏或删除。
- GLN 配置前必须识别楼层、墙体、Zone 和室内外关系。
- 最低结构条件不满足时，只输出转换报告，不得猜测房间或放置设备。

场景保存源文件路径、哈希和转换报告，不默认把大型 GLB/IFC 文件复制进数据库。
源文件丢失不影响已转换节点，但无法重新建立对照层。

## 9. AI 配置与本地 Codex

### 9.1 入口与边界

AI 同时提供：

- GLN Editor 内置面板。
- MCP 工具入口。

两种入口必须使用同一个 `ScenePlan` Schema、校验器和提交事务。

默认模型入口是用户本机安装并登录的 Codex。服务端通过受限桥接调用
`codex exec`，不在浏览器内嵌 OpenAI API Key，也不允许浏览器提交任意命令。
任务必须由用户主动触发，有白名单、超时、取消、进度和错误日志。此设计不用于
绕过安全规则、使用限制或滥用检测。

默认在本地解析 GLB/IFC。Codex 只接收结构化摘要和低分辨率视图；上传原始文件
必须由用户明确选择。

### 9.2 `ScenePlan`

AI 只能输出符合 JSON Schema 的场景修改计划：

```ts
type ScenePlan = {
  baseSceneId: string
  baseVersion: number
  intent: 'reconstruct-home' | 'configure-gln' | 'repair-gln'
  creates: PlannedNode[]
  updates: PlannedPatch[]
  deletes: PlannedDelete[]
  warnings: PlanWarning[]
}
```

处理流程：

```text
读取当前场景与版本
  -> 生成 ScenePlan
  -> Schema 与领域校验
  -> 半透明变更预览
  -> 用户确认
  -> 建立提交前恢复点
  -> 单次原子提交
  -> 建立提交后恢复点
```

一个计划只形成一个撤销步骤。失败时不得留下半成品。

### 9.3 AI 权限

AI 可以：

- 从零生成正常住宅节点。
- 在已存在住宅中放置和调整四类 GLN 资产。
- 配置系统、Zone、连接关系和安装位置。
- 修复未锁定的断连、碰撞和非法路径。

AI 不可以：

- 在“配置 GLN”任务中移动墙、门、窗、楼板或改变房间布局。
- 创建不可编辑整体模型。
- 覆盖 `lockedByUser: true` 的节点及位置。
- 编造设备性能和计算结果。
- 绕过校验直接写入场景。

再次运行配置时，按 `systemId` 与节点 ID 增量更新现有系统，不重复创建设备。
只有用户明确选择“新增一套系统”时才创建新的 `gln:system`。

### 9.4 提交硬校验

`ScenePlan` 必须全部通过：

- 注册节点 Schema。
- 基准场景版本。
- `systemId` 与 Zone 引用。
- 类型化端口和供回水方向。
- 拓扑完整性与断连。
- 安装区域和空间归属。
- 几何碰撞与管线路由。
- 面板贴墙和局部左右接口。
- 锁定节点保护。
- 重复设备检查。

任一硬校验失败都不得提交，只显示问题清单。

## 10. 保存与版本

### 10.1 独立数据库

GLN Editor 使用独立 SQLite 文件，不与原版 Editor 默认的
`~/.pascal/data/pascal.db` 混用。应用启动时显式设置 `PASCAL_DB_PATH`，例如：

```text
~/.pascal-gln/data/gln.db
```

数据库位置应允许通过环境变量覆盖。原版 Editor 的数据库和场景不迁移、不删除。

### 10.2 自动保存

沿用现有 Editor 保存模型：

- 场景变化后约 1 秒防抖自动保存。
- 页面关闭或刷新前补存。
- 使用版本号和 `If-Match` 乐观锁检测冲突。
- 防止场景从大量节点异常清空后被自动覆盖。
- UI 显示“待保存、保存中、已保存、已暂停、冲突、失败”。
- AI 批量操作前后创建 checkpoint；普通拖动写入 draft。

JSON 导入导出仅用于手动备份、迁移和故障恢复。第一版不要求 GLB 导出，也不建设
独立只读 Viewer。

保存内容包括节点、关系、系统模式、Zone 设定、插件安装状态和用户显示选项。
粒子播放位置、临时高亮和动画相位不持久化。

## 11. 编辑与运行预览

两种状态共享同一实时场景：

### 编辑视图

- 显示节点、端口、隐蔽管线开关、检修净空和碰撞信息。
- 流动动画弱化，避免遮挡操作。
- 允许拖动、参数修改、锁定、连接、撤销和重做。

### 运行预览

- 隐藏编辑辅助线。
- 展示供回水方向、面板能量传递方向和 Zone 目标设定。
- 不显示伪造的实测值或计算曲线。
- 允许切换 `cooling`、`heating` 和 `standby`。

水流方向来自拓扑。夏季能量视觉从空间指向面板，冬季从面板向空间表面扩散。

## 12. 设备预设与检修净空

第一版四类资产使用参数化 Three.js 几何。未来获得正式产品 GLB 后，只替换
renderer 外观，不改变节点 ID、端口、连接、尺寸参数和场景关系。

外机、水箱和面板支持产品预设。未指定型号时使用通用占位预设。

检修净空不使用未经验证的硬编码距离：

- 每个设备预设携带可编辑净空参数。
- 编辑视图用半透明体积显示净空。
- AI 放置时净空不得与建筑或其他设备冲突。
- 缺少产品资料时显示“净空待确认”。

## 13. 插件界面

GLN 面板至少包括：

### 系统

- 系统列表和当前 `systemId`。
- 新增系统、重命名、模式切换。
- 系统拓扑状态和待修复数量。

### 设备

- 放置外机、缓冲水箱和面板。
- 绘制供回水管。
- 选择产品预设。
- 锁定或解锁节点。

### Zone

- 仅列出安装面板的空间。
- 设置目标温度、目标湿度和启停状态。
- 标出“未设置”与“模板建议”。

### AI 配置

- 住宅重建。
- 配置一套系统。
- 新增系统。
- 修复当前系统。
- 查看计划、警告、变更预览和任务日志。

### 检查

- 显示断连、接反、碰撞、非法安装区和未知 Zone。
- 显示隐蔽管线。
- 显示检修净空。
- 点击问题定位节点。

## 14. 实施阶段

### 阶段 A：应用与插件基础

- 新增 `apps/gln-editor`，原 `apps/editor` 保持不变。
- 新增 `packages/plugin-gln` 并静态注册。
- 建立独立数据库配置。
- 完成注册表驱动的插件节点校验和持久化测试。

完成条件：测试插件节点可以创建、自动保存、刷新恢复和通过 MCP 修改。

### 阶段 B：领域节点与编辑

- 实现 `gln:system`。
- 实现四类参数化物理节点。
- 实现端口、工具、2D/3D 移动和检查器。
- 实现面板贴墙、Zone 归属、锁定与检修净空。

完成条件：所有资产均为可编辑节点，原子撤销和保存有效。

### 阶段 C：水系统拓扑

- 实现供回水干管、支管、立管和自动连接件。
- 实现吊顶路由、墙内下降和左进右出。
- 实现拓扑、断连、碰撞和删除影响检查。

完成条件：单层与多层住宅可形成可验证回路，非法路径不会被静默接受。

### 阶段 D：住宅重建

- 加固 IFC 到 Editor 节点转换。
- 新增 GLB 几何分析和 AI 重建。
- 实现置信度、复核队列、对照层与转换报告。

完成条件：目标建筑结构成为可编辑节点，低置信度结果不会被自动猜测。

### 阶段 E：本地 Codex 配置

- 实现受限 Agent 桥接。
- 定义 `ScenePlan` JSON Schema。
- 实现预览、硬校验、原子提交、恢复点和任务状态。
- 实现锁定保护和幂等更新。

完成条件：失败任务不污染场景，重复运行不产生重复设备。

### 阶段 F：即时可视化与收口

- 实现编辑视图与运行预览。
- 实现水流和能量传递动画。
- 实现 Zone 目标设定展示。
- 完成全量中文、响应布局、性能和视觉验收。

## 15. 测试策略

### Schema 与领域测试

- 每种节点默认值、范围和迁移。
- 插件注册表校验。
- `systemId`、Zone、模式唯一事实来源。
- 左进右出和端口类型。
- 未知型号与空目标值。

### 编辑工具测试

- 放置、移动、翻面、复制、删除、取消、撤销和重做。
- 面板贴墙及 Zone 重算。
- 管线路径点、分支、立管和端口吸附。
- 锁定节点不被用户误拖或 AI 修改。
- 2D/3D 适用行为一致。

### 拓扑和几何测试

- 单系统、多系统、单层和多层回路。
- 供回水接反、断连和孤立支路。
- 门窗、梁柱、墙体和检修净空碰撞。
- 删除节点后的待修复状态。

### 保存与并发测试

- GLN 与原版数据库隔离。
- 自动保存、关闭补存、异常清空保护。
- 乐观锁冲突。
- AI 前后 checkpoint 和单步撤销。
- JSON 备份往返一致。

### 导入与 AI 测试

- IFC 分类重建。
- GLB 高低置信度分流。
- 最低住宅结构门槛。
- `ScenePlan` Schema 和每项硬校验。
- AI 不修改住宅结构、不覆盖锁定节点、不重复创建设备。
- 原文件不授权上传时保持本地。

### 视觉与性能测试

- 参数化资产比例和安装位置。
- 面板贴墙、顶部接口、隐蔽管线。
- 夏冬能量方向。
- 编辑辅助信息不遮挡操作。
- 大型住宅中的选择、拖动和预览帧率。

## 16. 第一版验收标准

- 原版 `apps/editor` 可继续独立运行，行为和数据不受 GLN 影响。
- GLN Editor 使用独立数据库并能自动保存、刷新恢复。
- 住宅结构和全部 GLN 资产均为正常可编辑节点。
- 可从手工、AI、GLB 或 IFC 得到满足最低条件的住宅。
- 可建立一套或多套 GLN 系统，重复运行 AI 不产生重复资产。
- 外机、水箱、管线和面板满足已确认安装区域与碰撞规则。
- 水管走吊顶，面板顶部局部左进右出，支持多层立管。
- 有面板的 Zone 可以独立设置目标温湿度。
- 运行模式只存于 `gln:system`。
- 用户锁定节点受到保护。
- AI 计划通过预览和硬校验后一次提交，可一步撤销。
- 编辑视图和运行预览都直接使用实时场景。
- 视觉方向科学，不使用送风解释辐射面板，不伪造计算数据。
- UI 中文完整，无关键按钮、面板或提示被遮挡。

## 17. 后续待办

第一版稳定后单独立项：

- 负荷计算。
- 水力平衡与管径、流量、泵扬程计算。
- 真实温湿度与动态热过程演算。
- 露点、结露风险与控制联动。
- 正式产品库、厂家参数和选型。
- 施工图、材料清单和工程报告。
- 经确认需要后再评估 GLB 导出或独立展示端。
- 远程插件市场和动态安装。

## 18. 提交策略

建议按可审查的纵向提交拆分：

1. 独立 GLN App 与数据库配置。
2. 通用插件校验和持久化。
3. GLN 插件脚手架与 `gln:system`。
4. 四类物理节点。
5. 管路拓扑与安装约束。
6. GLB/IFC 可编辑重建。
7. 本地 Codex `ScenePlan` 流程。
8. 即时可视化、中文和验收测试。

通用基础设施与 GLN 业务不得混在同一提交。每个阶段都要验证原版 Editor 未加载
GLN 插件，且原版数据库未被访问或迁移。

## 19. 最终方案

```text
原版 Pascal Editor（保留）

独立 GLN Editor
  + Pascal 建筑编辑能力
  + 必装 plugin-gln
  + 四类可编辑物理资产
  + gln:system / Zone / typed ports
  + GLB/IFC 可编辑重建
  + 本地 Codex ScenePlan
  + 独立 SQLite 自动保存
  + 实时编辑与运行预览
```

这条路线保留 Editor 已有的 Three.js 编辑能力，把 GLN 领域知识约束在独立 App
和插件中，并为未来工程计算留出明确接口，而不让第一版伪装成已经完成的仿真软件。
